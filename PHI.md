# 予測Φ1--Φ4 の検定記録

第13章「情報構造の供給者：手続きによる決済」第13.6.3節で登録した四つの予測について、
データの取得と検定の過程を記録する。本稿の未解決の問題(13)、優先順位1位にあたる。

**本稿の実証はすべて第III部（法人企業統計、中小企業実態基本調査、人材サービス総合サイト）
で行っており、第II部の演繹そのものは一度も検定していない。** 予測Φ1--Φ4 はその初めての検定になる。

このファイルは作業の記録であり、本文ではない。
結論が出た項目のみ本文（第13章および第22章）に反映する。経緯は付録Dに書く。

---

## 到達点（2026-09-05 現在）

| 予測 | 内容 | 判定 | 節 |
|---|---|---|---|
| **Φ1** | 依存が少ないほど改修までの期間が長い | **棄却** | 1.8 |
| **Φ2** | $M(t)$ は途絶に先行して減少する | **支持** | 2.6 |
| **Φ3** | $\CCC$ の分布は非負に偏る | **検証不能** | 3.5 |
| **Φ4** | 族7は 7-4 を除いてほぼ現れない | **外れ**（族7 は稀だが、現れたのは 7-2 で 7-4 は 0 件） | 4.8 |

**第II部の演繹に対する初めての検定であった。** 四つのうち支持は一つである。

三つの失敗はいずれも別の形をとる。

- **Φ1** は単一のクラスタが結果を作っていた。
  クラスタ頑健標準誤差で扱うという事前の判断が誤っていた（第1.8節）
- **Φ3** は対抗仮説が識別されなかった。比較対象の選び方の誤りであり、
  第17.2節の予測Bと同型の不良設定である（第3.5節）
- **Φ4** は「実装できる」から「現れる」を導いていた。
  量的な部分（族7が稀）は当たり、型の部分（7-4 が残る）は外れた（第4.8節）

**取得はすべて無料枠で完結した。** 認証情報を一度も用いておらず、課金の経路が存在しない（第0.6節）。

**副次的に測れた量がある**（第7節）。$\lambda$ と手続きの継続期間であり、
命題12.2 の $1/\lambda_{\rm novel}$ に初めて上限が入った。
検定の過程で予測D と $\Phi$1 が同一の関係であることも判明した。

---

## 0. 共通の設計

### 0.1 区分「内」の判定

第13章の前提(2)は「$\mathcal{P}$ の実行結果を全参加者が独立に検証できる」ことである。
本文に明記したとおり、**検証者が限定される場合、および状態を特定の事業者が保持する場合は
前提(2)を満たさず、演繹は適用できない。**

したがって次を母集団から除く。**これは事前確定事項であり、結果を見てから変えない。**

| 除外するもの | 理由 |
|---|---|
| 中央集権取引所（DefiLlama の `CEX` 類） | 状態を事業者が保持する。台帳上の残高は事業者の保管であって手続きの状態ではない |
| 保管型のブリッジ・ラップ資産 | 同上。台帳外の資産に対する請求権 |
| 検証者が限定される台帳 | 前提(2)を満たさない |

DefiLlama の Ethereum プロトコル上位はCEXが占めるため、この除外は分布を大きく変える。

### 0.2 事前確定事項（第13.6.3節 表13.9）

| 項目 | 内容 |
|---|---|
| 母集団 | 基準時点に手続きの記録上で稼働していた $\Phi$ の全数。事後に選ばない |
| 下限 | 取引件数が基準期間に一定数未満のものを除く。閾値は取得前に確定する |
| 途絶の定義 | 一定期間にわたり取引が生じないこと。停止ではなく途絶で定義する |
| 定常状態 | $r$ が安定した期間に限る |
| 観測単位 | 識別子。主体単位への集計は行わない |
| 類型の割り当て | 第10.9節の判定順序を用いる。順序は変更しない |

### 0.3 解釈を縛る三つの制約（第13.6節・第13.7節）

いずれもデータ源の選択では解消しない。

1. **集計の不能。** 観測は識別子単位に限られ、一主体が複数の識別子を持つため
   主体単位の $\kappa_i$ に戻せない。障害分類の新たな形式にあたる（注13.5）。
2. **母集団が異なる。** 区分「内」で測った分布を企業一般に一般化できない。
   検証できるのは演繹が正しいかであって、演繹が現実の一人事業に当てはまるかではない。
3. **手続きが強制する量は検証にならない。** 検証の対象は事業者が選んだ結果として現れる量に限る。

### 0.4 データ源と到達性

いずれも 2026-09-04 に本環境から実際に応答を確認した。

| 源 | 用途 | 認証 | 到達 |
|---|---|---|---|
| `https://eth.drpc.org`（JSON-RPC） | `eth_getLogs` の履歴走査 | 不要 | ○ 窓5000ブロックまで。`User-Agent` が要る（無いと Cloudflare が 403 / error 1010）。**バッチ要求には 500 を返す** |
| `https://api.llama.fi`（DefiLlama） | TVL と手数料の系列、プロトコル一覧 | 不要 | ○ 無料枠31本、レート制限なし |
| `https://eth.blockscout.com` | 検証済みソース、ログ | 不要 | ○ |
| `https://sourcify.dev/server` | 検証済みソースの全数 | 不要 | ○ v1 は 2026-07-07 停止。v2 を使う |
| `https://api.etherscan.io/v2` | 検証済みソース | **APIキーが要る** | × 鍵未取得。無料枠は 5 req/s・10万/日 |
| `https://ethereum-rpc.publicnode.com` | `eth_getCode` のバッチ取得 | 不要 | ○ 500 件までのバッチに応じる（1,000 で 429）。履歴ログ（archive）は拒むが `latest` の `eth_getCode` は通る |
| `https://1rpc.io/eth` | — | — | × `eth_getLogs` が50ブロックまで |
| `https://rpc.ankr.com/eth` | — | — | × 要認証 |
| BigQuery `bigquery-public-data.crypto_ethereum` | traces の全数 | Google 認証が要る | 未使用 |

**Etherscan の API キーが要る場合は取得を依頼する。** 現時点では Blockscout と Sourcify で代替できている。

### 0.5 取得の実務で判ったこと

いずれも実行して確かめた。再取得する者が同じところで詰まらないよう記録する。

| 事項 | 内容 |
|---|---|
| `User-Agent` が要る | `eth.drpc.org` は `User-Agent` の無い要求を Cloudflare が 403（`error code: 1010`）で返す。`curl` は通り、Python の `urllib` の既定では通らない |
| 窓は 5,000 ブロックまで | 10,000 で 408 になる区間がある。密度に応じて 5,000--250 の間で伸縮させる |
| `Upgraded(address)` は indexed でない実装がある | 実装アドレスが `topics[1]` でなく `data` の先頭ワードに入る。両方を見る |
| 400 が返る区間がある | 再試行しても通らない窓がまれにある。飛ばした区間を記録し、後で窓を詰めて埋める |
| `/protocol/{slug}` は重い | 2--19 MB。大半がトークン別内訳なので、日次の TVL 系列だけ残して捨てる |
| DefiLlama の手数料は一部のみ | Ethereum で系列を持つのは 587 件。`/overview/fees/ethereum` で対象を先に確定する |

### 0.6 費用と無料枠

**課金は発生しない。請求の経路が存在しない。**

| 確認 | 結果 |
|---|---|
| 環境の認証情報 | `ETHERSCAN_API_KEY` `ALCHEMY_API_KEY` `INFURA_ID` `DRPC_KEY` `GOOGLE_APPLICATION_CREDENTIALS` `AWS_ACCESS_KEY_ID` `DUNE_API_KEY` いずれも未設定。gcloud の ADC も `~/.aws/credentials` も無い |
| 取得処理が送るヘッダ | `Content-Type` `User-Agent` `Accept` のみ。`api_key` `Authorization` `Bearer` `token` は一件も無い |
| 叩いたホスト | `eth.drpc.org`、`ethereum-rpc.publicnode.com`、`api.llama.fi`、`sourcify.dev`、`eth.blockscout.com`。いずれも無認証・無料 |

課金には識別が要る。アカウントも鍵も渡していない以上、
使用量を誰かに紐づける手段がなく、従量請求は成立しない。

**無料枠であることは API の応答が明示する。**

```
x-drpc-owner-tier: free
x-drpc-owner-id:   cb501251-...   （公開エンドポイントの共有 owner。当方の口座ではない）
```

**残枠を返す API は無登録の公開エンドポイントには無い。**
無登録には枠という概念がなく、あるのは IP 単位のレート制限だけだからである。
したがって残枠の確認の代わりに、この階層のヘッダを証拠とする。

| 項目 | 実績 | 公表上限 |
|---|---|---|
| Φ1 本走査 | 138.7 分で概算 4,400--8,000 リクエスト（0.5--1.0 req/s） | 120,000 CU/分/IP（`eth_call` 約 100 req/s 相当） |
| 総リクエスト | 約 7,300--10,900 件（drpc 約 4,800--8,400、DefiLlama 2,441） | -- |

意図的に 0.4--1.0 秒の間隔を入れており、上限の 1% 以下である。

Etherscan は鍵が無いため全要求が入口で弾かれ、何も消費していない
（`{"status":"0","message":"NOTOK","result":"Missing/Invalid API Key"}`）。
BigQuery と Dune は一度も使っていない。CLI も認証情報も無い。

**残る作業も無料枠で完結する。** 枠B（約 300 リクエスト、90 MB）と
検証済みソースの取得（Sourcify・Blockscout、いずれも無認証）だけであり、
新たに鍵を要する経路は使わない。


---

## 1. 予測Φ1：依存の数と改修までの期間

> 手続きが呼び出す他の手続きの数が少ないほど、改修されるまでの期間が長い。
> 命題12.2（無人化は期間の関数である）の $\lambda_{\rm dep}$ 側の含意である。
>
> **対抗仮説**：改修の頻度は依存の数ではなく利用量で決まる。依存が少なくとも利用が多ければ改修される。

### 1.1 文献の確認（規律1）

Qasse, Hamdaqa, Jónsson (2024)
[*Immutable in Principle, Upgradeable by Design: Exploratory Study of Smart Contract Upgradeability*](https://arxiv.org/abs/2407.01493)
を精読した（PDF は `data/raw/phi/lit/`）。

| 項目 | 内容 |
|---|---|
| 母集団 | Ethereum の配備済みコントラクト 44,000,000 件。Ethereum ETL（BigQuery）＋ Etherscan |
| 改修可能の同定 | プロキシ型のみ。EIP-1967、EIP-1822 (UUPS)、EIP-2535 (Diamond)、Beacon。移行型とデータ分離型は除外 |
| 結果 | 改修可能は 3%（1,300,000 件）。**うち実際に2回以上改修されたのは 4,397 件**、版は 14,990 |
| 改修イベント | 9種を同定。`Upgraded(address)` が最多で 3,715 件 |
| RQ4 の設計 | 被説明変数＝取引件数、説明変数＝版の寿命と版番号。線形で $R^2=-0.03$、ランダムフォレストで $R^2\approx0.358$ |
| 公開 | ソースとデータセットを公開（プロジェクトリポジトリ） |

**予測Φ1 と重ならない。** この論文の設計は「改修 → 活動」であり、
予測Φ1 は「依存 → 改修までの期間」で向きが逆である。
**依存の数は説明変数に入っていない。** 被説明変数も取引件数であって改修までの期間ではない。

ただし三点を引き継げる。

- **改修イベントの同定規則。** 9種のイベント署名は本稿でもそのまま使える
- **`Upgraded(address)` は配備時にも発火する。** 論文は改修可能プロキシ 1.3M に対し
  実際の改修を 4,397 と数えている。この差は配備時の発火を除いた結果である。
  **アドレスごとの1回目は配備、2回目以降が改修**として扱う必要がある
- **検証済みコントラクトに限っている。** 未検証を落とすと標本が偏る。本稿は落とさない

対抗仮説（利用量）についても、この論文の RQ4 が「改修と利用活動の関係は複雑」と
報告しているだけで、統制としては使えない。統制は自前で設計する。

### 1.2 必要なデータと形式

| 変数 | 操作的定義 | 源 | 形式 |
|---|---|---|---|
| 母集団 | `Upgraded(address)` を発火した全アドレス（＝改修可能プロキシ） | `eth_getLogs` | (address, block, implementation) |
| 改修事象 | 同一アドレスの2回目以降の発火 | 同上 | (address, block, 版番号) |
| 改修までの期間 | 配備ブロック〜最初の改修ブロックの時間差。右打ち切りあり | 同上＋ブロック時刻 | 日数、打ち切り指標 |
| 依存の数 | 実装バイトコードの `PUSH20` アドレス数（第1.4節で確定） | `eth_getCode` | 整数 |
| 利用量（統制） | 枠B と同じ基準期間の記録の件数（第1.4節で確定） | `eth_getLogs` | 整数 |

イベント署名は本環境で `keccak256` を計算して確認した。

```
0xbc7cd75a20ee27fd9adebab32041f755214dbc6bffa90cc0225b39da2e5c2d3b  Upgraded(address)
```

（検算：`Transfer(address,address,uint256)` が既知の
`0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef` と一致した）

### 1.3 データ量の見積もり（無料枠内）

`eth.drpc.org` に対し 25 窓 × 1,000 ブロックを block 5,000,000 から現在（25,903,413）まで
等間隔に標本抽出して密度を測った（`data/raw/phi/phi1_density.json`）。

| 量 | 値 |
|---|---|
| 標本 | 25,000 ブロック、`Upgraded` ログ 2,267 件、1.18 MB |
| 外挿 | 全期間で約 1,900,000 件 |
| 生 JSON | 約 1.0 GB |
| 保存形式（address + block + implementation のみ） | **約 0.18 GB** |
| リクエスト数（窓5,000） | 約 4,200 |
| 所要 | 約 1--2 時間（密な区間は窓を詰めるため延びる） |

**無料枠に収まる。** 推定 1.9M 件は論文の改修可能プロキシ 1.3M と整合する
（各プロキシが配備時に1回以上発火するため）。

**走査中の実測（2026-09-04 時点、77% 通過）はログ 1,653,000 件**であり、
見積もりとよく合っている。57%→69% の区間で増えたのは約 4.4 万件にとどまり、
2022年9月以降が疎であるという密度測定の結果を裏づけている。
最終は 170--180 万件に落ち着く見込みである。

密度は一様でない。2018年以前は0、2021--2022年に集中し、
2022年9月以降は疎になったのち2025年9月に再び跳ねる。
単純な外挿の分散は大きいが、桁は確かである。

### 1.4 説明変数と統制の定義（**確定。取得前に固定した**）

注13.15 が「統制の手続きを取得前に確定しておく」ことを課している。
以下は結果を見る前に固定した。**あとから変えない。**

#### 依存の数

**実装コントラクトのバイトコードに `PUSH20` で埋め込まれた、相異なるアドレスの数。**
ゼロアドレスと、$2^{32}$ 未満の値（アドレスではなく定数とみなす）は除く。

三案を実測して比べた（改修が観測されたプロキシから種を固定して 30 件）。

| 案 | 取得できた率 | 採否 |
|---|---|---|
| (a) traces から相異なる `to_address` | **0%**。公開RPCでは取れず archive か BigQuery（要認証）が要る | 否 |
| (b) 検証済みソースの外部 interface | **63%**（19/30） | 否 |
| (c) バイトコードの `PUSH20` アドレス | **97%**（29/30） | **採用** |

(b) を採らないのは、4割が落ちて標本が偏るためである。
**これは第1.1節で引いた論文が抱えた偏りそのもの**であり、本稿は未検証を落とさない。

分散は十分にある（中央値 38、最小 3、最大 94、ゼロは 0 件）。

**限界を明記する。これは依存の下限である。**
実行時に引数やストレージから受け取るアドレスは数えられない。
逆に、埋め込まれていても呼ばないアドレスを過大に数えうる。
したがって「依存の数」ではなく**静的に固定された依存の数**と呼ぶ。

#### 利用量（統制）

**第4.4節の枠B と同じ基準期間・同じ抽出で数えた、当該アドレスの記録の件数。**
追加の取得を要さない。回帰には $\log(1+\text{件数})$ を入れる。

#### 観測単位とクラスタ

事前確定は「観測単位は識別子」と定めている。これは変えない。

ただしデータの構造として、**192 万のプロキシが指す実装は 76,609 種しかない**（4.0%）。
うち一つ（`0xf9e266…` ＝ OpenSea 旧取引所の `AuthenticatedProxy`）を
**1,548,584 個、すなわち母集団の 80% が指している**。
これらは独立な 150 万の $\Phi$ ではなく、一つの $\Phi$ の 150 万の複製であり、
同一のバイトコード・同一の依存数を持つ。

**識別子を観測単位に保ったまま、実装アドレスをクラスタとする頑健標準誤差で扱う。**
事前確定の文言を変えずに済む。クラスタ構造は結果ではなくデータの構造であるから、
それを見てから決めても事後選択にはあたらない。

実装が 76,609 種しかないことは取得量にも効く。
依存の数は実装で決まるので、76,609 件を取れば 192 万件すべてに値が付く。
`publicnode` のバッチ（500 件）を使えば **154 リクエスト、8 分、受信 2 GB**である。
バイトコードは中央値 11,989 B、平均 12,895 B、最大 24,547 B。
保存するのは件数だけなので 76,609 行の TSV（約 3 MB）に収まる。

### 1.5 走査の結果

block 4,000,000 から 25,903,466 まで、138.7 分で完了した。

| 量 | 値 |
|---|---|
| `Upgraded(address)` のログ | **1,983,047 件** |
| 相異なるアドレス（＝改修可能プロキシ） | **1,928,769 件** |
| うち改修を観測（2回目以降の発火あり） | **24,307 件** |
| 右打ち切り（配備のみ） | 1,904,462 件 |
| 改修までの日数 | 中央値 24.1 日、平均 120.3 日 |
| 保存 | 186 MB |

事前の外挿（190 万件）とよく合った。

**論文との対照。** Qasse らは改修可能プロキシ 1.3M、実際に改修されたもの
4,397 件を報告している。本稿は 1.93M と 24,307 件である。差は二つ。
観測期間が 2026 年まで延びていること、
そして**本稿は未検証コントラクトを落としていない**ことによる。

改修回数の分布は 1 回が 14,592、2 回が 4,782、3 回が 1,705、4 回が 954 と
急速に減る。第4分位以降は薄いので、検定は初回改修までの期間で行う。

### 1.6 取り逃した区間（補充済み）

全数を謳う以上、穴を残さない。本走査で 8 区間・計 2,000 ブロックが飛んだ。

| 事由 | 件数 |
|---|---|
| `method handler crashed`（`-32000`） | 6 |
| `HTTP 400 Bad Request` | 1 |
| `HTTP 408 Request Timeout` | 1 |

窓を 25→5→1 ブロックと詰めて再取得し、**全区間を埋めた（323 件を追記）**。
いずれも一時的な失敗であり、区間そのものに問題はなかった。
**母集団に穴はない。**（`data/scripts/phi/phi1_backfill.py`）

### 1.7 推定の方法

事前確定は Cox を指定していないので、比例ハザードの枠内で数値的に通る方法を選ぶ。

**lifelines の Cox は収束しなかった**（Newton--Raphson が NaN）。
193 万行に対し相異なる期間が 15.9 万しかなく、同値のタイが極端に多いためである
（最大のタイは 2,623 行）。

**区分指数モデル（piecewise-exponential）に替えた。**
追跡期間をイベント時刻の十分位で 10 区分に切り、区分ごとに一定のベースラインを置く。
Cox のベースラインを区分定数にした場合と等価であり、タイに対して安定する。
説明変数は実装ごとに一定なので、$(\text{実装}\times\text{区分})$ に集計しても情報は失われない
（セル 369,290 行）。標準誤差は第1.4節のとおり実装をクラスタとする頑健標準誤差。

区分の切れ目（日）は 0, 0.2, 2.6, 8.4, 21.2, 42.2, 62.9, 104.8, 206.1, 454.9, 3076.6。

### 1.8 結果：**予測Φ1 は棄却された**

| モデル | $n$ | 改修 | `log_dep` の係数 | HR | $p$ |
|---|---|---|---|---|---|
| 主：区分指数（全体） | 1,925,170 | 20,732 | $+1.567$ | 4.79 | $0.0036$ |
| 主：**支配的クラスタを除く** | 376,617 | 18,989 | $\mathbf{-0.228}$ | 0.80 | $\mathbf{0.45}$ |
| 副：実装単位（全体） | 45,726 | 20,732 | $+1.640$ | 5.16 | $0.0004$ |
| 副：**支配的クラスタを除く** | 45,725 | 18,989 | $\mathbf{-0.023}$ | 0.98 | $\mathbf{0.96}$ |

**45,726 のクラスタから 1 つを外すだけで、$p=0.0036$ が $p=0.45$ になり符号も逆転する。**

依存の四分位ごとの改修率（支配的クラスタを除く、実装単位、実装年あたり）。

| 依存の数 | 実装数 | 改修 | 曝露（年） | 改修率 |
|---|---|---|---|---|
| 0--16 | 12,092 | 5,536 | 79,606 | **0.0695** |
| 17--26 | 11,745 | 4,282 | 131,446 | 0.0326 |
| 27--38 | 10,595 | 3,845 | 59,704 | 0.0644 |
| 39--301 | 11,293 | 5,326 | 120,931 | 0.0440 |

**単調でない。** 依存が最も少ない群の改修率が最も高く、予測とは逆向きである。

**見かけの支持を作っていたもの。**
`AuthenticatedProxy` は依存 3 個、複製 150 万、曝露 744 万実装年、改修 1,747 件で、
改修率は 0.00098／実装年、他の実装の 30--70 分の 1 である。
「依存が少ないほど改修が遅い」という関係は、この 1 件が
「依存 3・改修が極端に遅い」という点を占めていることの言い換えにすぎない。
これは 2018 年の OpenSea 旧取引所が利用者ごとに配ったコントラクトであり、
**取引所自体が 2022 年に停止したため改修されなくなった**。
依存が少ないから改修されなかったのではない。

**対抗仮説も支持されない。** 利用量の係数は $-0.35$（$p=0.39$）である。
ただし統制がほぼ動かないので強い検定ではない（下記1.9）。

> **クラスタ頑健標準誤差では防げなかった。**
> 第1.4節で「識別子単位のまま、実装をクラスタとする頑健標準誤差で扱う」と確定したが、
> これは効かなかった。頑健標準誤差はクラスタ内の相関を補正するが、
> **一つのクラスタが曝露の 95% を占める**場合の点推定の偏りは補正しない。
> 判断が誤っていた。支配的クラスタを外した推定を併記して初めて分かった。
> 同型の構造（少数の工場が大量の複製を配る）は台帳上の $\Phi$ では珍しくない。
> **今後この領域で標本を組むときは、複製の集中を先に数える。**

### 1.9 この結果が言っていないこと

- **依存の数は下限である。** 静的に固定されたものだけを数えており、
  実行時に引数やストレージから受け取るアドレスは数えていない（第1.4節）
- **統制がほぼ動かない。** 利用量が正なのは 193 万行のうち 819 行（0.043%）である。
  枠B は 2026 年 8 月の 224 ブロックしか見ないので、
  何年も前に配備されて休眠しているプロキシは 0 になる。
  対抗仮説を強く検定するには生涯取引件数のような別の指標が要り、追加取得を要する。
  **定義は変えない。** 注13.15 が取得前の確定を課しており、
  結果を見てから差し替えれば事前登録が無効になる
- **`duration=0` を 3,552 件除いた。全改修の 14.6% にあたる。**
  配備と同一ブロックでの 2 度目の発火であり、
  後からの改修ではなく配備時の初期化である。期間 0 は生存モデルに入らない
- **棄却は「依存が改修に効かない」ことを示すが、
  命題12.2 そのものを否定しない。** 命題は $\lambda_{\rm novel}$ 全体について述べており、
  本検定はその $\lambda_{\rm dep}$ 成分を、静的な依存という一つの代理で測ったにすぎない

### 1.10 進捗

- [x] 文献の確認
- [x] 密度の測定と見積もり
- [x] `Upgraded(address)` の全期間走査（198 万件）
- [x] 取り逃した区間の補充（全区間）
- [x] 依存の数・統制・観測単位の定義を確定（第1.4節）
- [x] 実装バイトコードの取得（76,609 件、62.5 分）
- [x] 依存の数の算出とパネルへの結合（100.0% の行に付いた）
- [x] **検定（棄却）**
- [ ] 本文への反映

## 2. 予測Φ2：$M(t)$ は途絶に先行して単調に減少する

> 取引が途絶えた $\Phi$ について、$M(t)$ は途絶に先行して単調に減少する。
> $\tau=\inf\{t:M(t)<0\}$ が吸収状態であることの含意である。
>
> **対抗仮説**：途絶は $M(t)$ と無関係に生じる。$M(t)$ は途絶の直前まで変化しない。

### 2.1 循環の危険（規律2）

**途絶を $M(t)$ で定義してはならない。**
$M(t)\to 0$ を途絶と呼べば「途絶に先行して $M(t)$ が減る」は定義から自明になり、
過去に2件撤回した循環する証明と同型になる。

第13.6.3節の事前確定は途絶を「一定期間にわたり取引が生じないこと」と定めている。
**取引の途絶（フロー）と $M(t)$（ストック）は別の系列から取る。**

| 量 | 系列 | 源 |
|---|---|---|
| $M(t)$ | 手続き上に置かれた資産（TVL） | DefiLlama `/protocol/{slug}` |
| 活動 | 手数料の発生（フロー） | DefiLlama `/summary/fees/{slug}` |

手数料は取引が生じたときにのみ発生するので、途絶の代理になる。
**ただし手数料は通常 TVL と相関するため、完全な独立ではない。**
より望ましいのは識別子ごとの最終取引時刻だが、
プロトコルから構成コントラクトへの対応づけが要る。次の段階の課題とする。

### 2.2 $M(t)$ と TVL の対応

$M(t) = E(t) + \sum_i\max\{-\kappa_i,0\} - \sum_i\max\{\kappa_i,0\}$（式(3.x)）。

TVL は手続き上に置かれた資産であり、$E(t)$ と受け取った預り（$\kappa_i<0$）の和にあたる。
**利用者の預りを含むため、事業者の自己資金そのものではない。**
命題13.2（同時性）により区分「内」では $\kappa=0$ の実装が存在するが、
実在の手続きの多くは預りを持つため $\kappa\neq 0$ である。

この差は解釈を縛る。予測Φ2 が言うのは経路制約の吸収性であり、
TVL で測る限り「手続き上の資産が尽きる前に活動が止まるか」を見ていることになる。
**代理であることを結果に明記する。**

### 2.3 必要なデータと形式

```
data/raw/phi/llama_protocols.json   プロトコル一覧 8,179 件（うち Ethereum 1,852）
data/raw/phi/llama_tvl.jsonl        {"slug","category","chains","listedAt","tvl_eth","tvl_all"}
data/raw/phi/llama_fees.jsonl       {"slug","fees":[[date,usd],...]}
```

`/protocol/{slug}` は 2--19 MB あり、トークン別内訳が大半を占める。
`phi2_fetch_llama.py` は日次の TVL 系列とメタデータのみを残して捨てる。
通信量は約 5.5 GB、保存は 64 MB（TVL、1,565 件時点）と 7.5 MB（手数料、完了）。

手数料の系列を持つのは Ethereum で 587 件のみで、`/overview/fees/ethereum` で
対象を先に確定してから取得した。TVL は 1,852 件すべてを対象とする。
**したがって途絶を判定できるのは両方を持つ 587 件までである。**
残りは TVL しか無く、循環を避ける以上そのままでは途絶を判定できない。

### 2.4 母集団の性質

DefiLlama の Ethereum プロトコルは 1,852 件。うち現時点の TVL が 0 または欠測が 229 件で、
これが途絶の候補になる。ただし——

- **DefiLlama に載っているのは主に金融の手続きである。** 分野に強く偏る
- **CEX を除く（0.1節）。** TVL 上位は CEX が占めるため除外の影響が大きい
- **登録時刻 `listedAt` の最古は 2021-10-12** で、それ以前に途絶したものは載っていない可能性がある。
  生存条件づけが再発しうる。第13.6節が「手続きの記録そのものが母集団になる」と述べた利点は、
  DefiLlama を使う限り実現しない

### 2.5 途絶の定義（**確定。集計前に固定した**）

> **途絶** ＝ 手数料が **90 日以上**連続してゼロであり、**かつ最後まで再開しない**こと。
> その最初の日を途絶の日とする。途絶の前に **180 日以上**の活動を要求する。

閾値は結果（TVL の傾き）を一切見ずに、手数料系列そのものの構造から決めた。

**あとで再開したゼロの連なり（＝一時的な休止）の分布**（$n=14{,}911$）

```
中央値 2 日   90%点 7 日   99%点 45 日   最大 1,229 日
```

| 閾値 | 休止を途絶と誤る件数 | 誤判定率 | 途絶として拾える件数 |
|---|---|---|---|
| 30 日 | 257 | 1.72\% | 101 |
| 60 日 | 95 | 0.64\% | 83 |
| **90 日** | **56** | **0.38\%** | **70** |
| 120 日 | 35 | 0.23\% | 62 |
| 180 日 | 17 | 0.11\% | 44 |

一時的な休止の 99% 点が 45 日であるから、60 日以上は通常の休止の外側にある。
90 日は余裕を見た値であり、誤判定 0.38% と引き換えに 70 件を確保する。

**「かつ最後まで再開しない」を条件に加えた理由。**
これを欠くと、系列の途中にある長い休止も途絶として拾ってしまう。
2022 年に 100 日休んで今も稼働している $\Phi$ が途絶に入る。
途絶は停止ではなく途絶で定義するという事前確定（第0.2節）に沿えば、
再開したものは途絶ではない。

**継続群との対照を必ず置く。**
途絶しなかった $\Phi$ について、観測末尾から同じ窓で傾きを測る。
これを欠くと、TVL が全体として減少基調である可能性を排除できない。

### 2.6 結果：**予測Φ2 は支持された**

第2.5節で確定した定義で集計した。

| 群 | 件数 |
|---|---|
| 判定できた母数（TVL と手数料の両方を持つ） | 427 |
| 途絶 | **40** |
| 継続 | 387 |

基準日から遡る窓での $\log$ TVL の日次の傾き。
途絶群の基準日は途絶の日、継続群の基準日は観測の末尾である。

| 窓 | $n$（途絶／継続） | 中央値（途絶） | 中央値（継続） | Mann--Whitney $p$ | 負の割合（途絶／継続） | Fisher $p$ |
|---|---|---|---|---|---|---|
| 180 日 | 31 / 365 | $-0.00368$ | $-0.00125$ | $7.6\times10^{-3}$ | 77.4\% / 72.1\% | $0.34$ |
| 90 日 | 32 / 366 | $-0.00505$ | $+0.00154$ | $7.0\times10^{-7}$ | 75.0\% / 35.8\% | $1.8\times10^{-5}$ |
| **30 日** | 32 / 364 | $-0.00298$ | $+0.00644$ | $\mathbf{3.2\times10^{-9}}$ | **75.0\% / 24.7\%** | $2.0\times10^{-8}$ |

いずれも片側。「途絶群の傾きは継続群より小さい」「途絶群のほうが負の割合が高い」を対立仮説とする。

**対抗仮説は棄却される。** 対抗仮説は「途絶は $M(t)$ と無関係に生じ、
$M(t)$ は途絶の直前まで変化しない」であった。
途絶前 30 日で、途絶群の 75.0% が減少しているのに対し継続群は 24.7% であり、
中央値は $-0.0030$ と $+0.0064$ で符号が逆である。

窓を狭めるほど分離が鋭くなる（$p$ が $10^{-3}\to10^{-7}\to10^{-9}$）。
減少が途絶の直前に集中していることを意味し、経路制約の吸収性と整合する。

> **注記。**
**継続群を置いたことが判定を分けた。**
180 日窓では継続群も 72.1% が負であり、**分離しない**（Fisher $p=0.34$）。
対照を欠いたまま「途絶前 180 日で 61.9% が負」だけを見れば支持と読んだはずだが、
それは TVL 全体の減少基調をなぞっていたにすぎない。
第2.5節で対照を必ず置くと決めておいたことが効いた。


### 2.7 この結果が言っていないこと

- **「単調に減少」は示していない。** 示したのは「継続群より有意に減少している」である。
  予測の文言は単調性を言っているので、判定を書くときに区別する
- **母数は 427 件である。** 手数料系列を持つものだけであり、
  残り 1,425 件は TVL しか無く判定できない
- **TVL は $M(t)$ の代理である。** 利用者の預りを含む（第2.2節）
- **DefiLlama の登録は 2021-10 以降**であり、それ以前に途絶したものは載らない。
  生存条件づけが完全には消えていない（第2.4節）

### 2.8 進捗

- [x] プロトコル一覧の取得（8,179 件、うち Ethereum 1,852 件）
- [x] 循環の危険の同定と、フロー系列を別に取る設計
- [x] 手数料系列の取得（587/587）
- [x] TVL 系列の取得（1,852/1,852、64 MB）
- [x] 途絶の定義を確定（第2.5節）
- [x] 確定した定義での集計と、継続群との対照
- [x] **検定（支持）**
- [ ] 本文への反映

## 3. 予測Φ3：$\CCC$ の分布は非負に偏る

> 区分「内」で稼働する $\Phi$ の $\CCC$ の分布は非負に偏る。
> 系13.12（成長には資本が要る）より、$\CCC<0$ の実現には $E>0$ を要するためである。
>
> **対抗仮説**：$\CCC$ の分布は第III部の企業部門と同様であり、区分「内」に固有の偏りはない。

### 3.1 障害は取得ではなく定義にある

$\CCC=\sum_i\tau_i+\tau_{\rm inv}$ であり、$\tau_i$ は履行から決済までの遅れである。
区分「内」では $\delta$ と $\pi$ がともに状態遷移なので $\tau_i$ は原理的に直接測れる。

**しかしどの状態遷移が $\delta$ でどれが $\pi$ かの対応づけは自動化できない。**
これが Φ3 の作業の実質すべてである。手続きごとに $\delta/\pi$ を同定する規則が要り、
それを結果を見てから決めれば事前登録が無効になる。

### 3.2 予測が問うているのは負の裾である

系13.12（成長には資本が要る）が言うのは $\CCC<0$ の実現に $E>0$ を要することである。
したがって検定の対象は**分布全体の位置ではなく負の側の薄さ**にある。

$\CCC<0$ とは決済が履行に先行すること、すなわち前受けである。
第III部の企業部門には前受型の業種が実在し、$\CCC$ の負値が観測される。
区分「内」でそれが希薄なら予測が支持される。

これは分布が0に集中しても検定として成立する。
命題13.2（同時性）により区分「内」では $\kappa_i=0$ の実装が存在するので、
手数料を履行と同一の遷移で受け取る手続きは $\tau_i=0$、すなわち $\CCC=0$ になる。
**0 は非負であり、企業部門の分布とも異なる。** 対抗仮説とは区別できる。

負値をとりうる手続きの例は、権利料を先に受け取り後に給付するもの
（族2-5 のオプション・保証と同型）、前払いの利用権、ボンディングである。

### 3.3 Φ4 と標本を共有する

$\delta/\pi$ の同定も類型の割り当ても、どちらも手続きの内容を読む作業である。
**同じ標本に対して一度の判定で両方を埋められる。**

`data/raw/phi/phi4_frame_a.tsv` に判定用の列を設けてある。

| 列 | 埋めるもの | 予測 |
|---|---|---|
| `族` `型` | 第10.9節の判定順序の結果 | Φ4 |
| `根拠` | どの判定で決まったか | Φ4 |
| （追加予定）`tau_符号` | $\pi$ が $\delta$ に先行するか、同時か、後れるか | Φ3 |
| （追加予定）`tau_日数` | 後れる場合の平均的な遅れ | Φ3 |

### 3.4 標本の判定（Φ4 と共通）

枠B の上位 200 件のうち、$\Phi$ と判定できたのは 52 件である（第4.8節）。
族別の内訳は族6 が 38、族3 が 9、族2 が 3、族5 が 1、族7 が 1。

### 3.5 結果：**予測Φ3 は検証不能**

理由は二つあり、いずれも追加のデータ取得では解消しない。

#### (1) 対抗仮説が識別されない

予測と対抗仮説は次であった。

> **予測Φ3**：区分「内」で稼働する $\Phi$ の $\CCC$ の分布は非負に偏る
> **対抗仮説**：$\CCC$ の分布は第III部の企業部門と同様であり、区分「内」に固有の偏りはない

比較対象である企業部門の $\CCC$ を実際に見ると、次のとおりである
（法人企業統計 2024年度、全規模、58業種）。

| 統計量 | 値（日） |
|---|---|
| 最小 | 4.9 |
| 25%点 | 20.1 |
| 中央値 | 33.6 |
| 75%点 | 41.6 |
| 最大 | 210.3 |
| **$\CCC<0$ の業種** | **0 / 58 ＝ 0.0%** |

**企業部門にも負の $\CCC$ が一つも無い。**
予測と対抗仮説が同じことを言っており、「非負に偏る」を観測しても対抗仮説を棄却できない。
**予測が不良設定である。**

これは第17.2節の予測Bと同型の欠陥である。予測Bは $\delta$ の単位が存在しないために
無人化率が定義できなかった。今回は単位ではなく**対照が識別されない**という形をとる。

**なぜそうなったか。** 系13.12 は「$\CCC<0$ の実現には $E>0$ を要する」と述べる。
そこから「区分『内』では $\CCC<0$ が希薄」を導いたが、
**負の $\CCC$ は個別企業（前受金型の事業）に現れるもので、業種の集計では相殺されて消える。**
比較対象の選び方を誤っていた。個票が要るが、e-Stat は集計値しか出さない。
第III部が繰り返し記録してきた障害がここにも現れている。

#### (2) $\tau$ の桁が違う

仮に対照が取れても、$\tau$ を同じ尺度に載せられない。

判定できた 52 件のうち族6 が 38 件（73%）を占め、その中に二種が混在する。

| 仕組み | $\tau$ | 例 |
|---|---|---|
| 同一の状態遷移で完結 | $0$ | Uniswap の各プール、Seaport、集約器 |
| 跨チェーン | 送信側で先払いか、解決者が立て替えるかで**符号が逆になる** | LayerZero（`EndpointV2`、`SendUln302`、`ReceiveUln302`）、CCIP（`OffRamp`）、Mayan、Relay |

跨チェーンの $\tau$ は**秒から分のオーダー**（$\approx 10^{-3}$ 日）であり、
企業部門の日から月とは桁が五つ違う。同じ「日」という単位に載せても比較にならない。

符号を決めるには仕組みを一件ずつ読む必要がある。
第3.1節で「$\delta$ と $\pi$ の対応づけは自動化できない」と述べたとおりであった。

### 3.6 進捗

- [x] 検定の対象が負の裾であることの確認
- [x] Φ4 と標本を共有する設計
- [x] 標本の判定（52 件）
- [x] 比較対象（企業部門の $\CCC$ 分布）の確認
- [x] **判定（検証不能）**
- [ ] 本文への反映

### 3.7 個票を取る道は採らない

集計値では対照が作れないので、個票を別口で集めて自前で集計する道を検討した。
**採らない。**

| | 法人企業統計（e-Stat） | EDINET（有価証券報告書） |
|---|---|---|
| 母集団 | 営利法人等 **約 89 万社** | 有報提出会社 **約 4 千社** |
| 手元の観測 | 1,364 セル | 約 10 万社年 |
| 粒度 | 集計値のみ | 個票 |

**観測の行数は約 70 倍になるが、母集団は 3 桁狭くなる。**
上場会社に限られるため、第20章が行っている規模の横断
（資本金 1 千万円未満から 10 億円以上まで）が成り立たず、
第19章の個人企業との比較もできない。
しかも上場は第15章が名指しした生存条件づけの塊であり、
そこだけを見て「$\CCC<0$ は希薄」と言えば枠組みの循環になる。

**労力に見合わないと判断した。** 既存の結果を置き換えるのではなく
第二の実証系列を足すことになり、規模は第20章に匹敵する。
得られるのは Φ3 の対照と未解決の問題(8)（企業年齢）程度である。

なお EDINET API v2 は無料だが subscription key の登録が要る
（v1 は廃止、`403`）。J-Quants も登録が要る（`403`）。
**この判断は結果を見て変えない。**

## 4. 予測Φ4：族7は 7-4 を除いてほぼ現れない

> 稼働する $\Phi$ を第10.9節の規則で割り当てると、族7は 7-4 を除いてほぼ現れない。
> 命題13.6（受益者の同定を要する $\Phi$ は実装できない）の含意である。
>
> **対抗仮説**：族7型も他の族と同程度の比重で現れる。

### 4.1 判定順序（第10.9節 表10.8、変更しない）

| 順 | 判定 | 該当すれば |
|---|---|---|
| 1 | 受益者と支払者が異なるか（自由度(3)） | 族7 |
| 2 | 第三者間の取引に介在するか | 族6 |
| 3 | $\pi$ が $\omega$ に可測か（自由度(4)） | 族5 |
| 4 | 履行を分割し $\pi$ を意図的に偏らせるか | 族4 |
| 5 | 資産が先行し、時間または稼働に比例して回収するか | 族3 |
| 6 | 反復性があるか（自由度(6)） | 族2、なければ族1 |

### 4.2 母集団の偏りが結果を決めてしまう危険

**族7は寄付・助成・広告など、対価関係が当事者間で閉じない $\Phi$ である。**
DefiLlama は金融の手続きを集めた一覧であり、族7型はそもそも載りにくい。
この枠だけを使えば予測Φ4 は**枠の選び方によって自明に支持される。**

したがって二つの枠を用意し、両方で判定する。

| 枠 | 内容 | 性質 |
|---|---|---|
| A | DefiLlama の Ethereum プロトコル（CEX 等を除く） | 金融に偏る。族7 に不利 |
| B | 基準期間に事象を記録した識別子（分野中立） | 未取得。`eth_getLogs` のアドレス集計で構成できる |

枠Bの構成が予測Φ4 の成否を分ける。**枠Aだけで結論を出さない。**

### 4.3 枠A の整理（済）

`data/scripts/phi/phi4_frame_a.py` → `data/raw/phi/phi4_frame_a.tsv`。
Ethereum を含む 1,852 件に 0.1節の除外規則を当てた。

| 区分 | 件数 | 内容 |
|---|---|---|
| 除外 | 79 | `CEX`。状態を事業者が保持するため前提(2)を満たさない |
| 要確認 | 325 | `Bridge` `Canonical Bridge` `Cross Chain Bridge` `RWA` `Staking Pool` `Services`。保管型かどうかがカテゴリだけでは決まらない |
| 対象 | 1,448 | |

判定の対象は 1,773 件（TVL>0 が 1,518、TVL=0 が 255）。

**カテゴリ名だけでは保管の有無が決まらない。**
たとえば `binance-staked-eth` はカテゴリが `Liquid Staking` だが運営は取引所であり、
実質は保管型である。要確認に落として人手で判定する。

記述の充足は `category` 1,852/1,852、`description` 1,851/1,852、
`methodology` 899/1,852、`url` 1,372/1,852、`address` 1,062/1,852。

カテゴリ上位（Ethereum）は Yield 211、Dexs 190、Lending 163、Bridge 100、RWA 82、
Yield Aggregator 80、CEX 79、CDP 70、Canonical Bridge 62、Risk Curators 54。
**族7に対応しうるカテゴリが一つも上位に現れない。** 4.2の懸念が現実であることを示す。

### 4.4 枠B の構成規則（**確定。取得前に固定した**）

分野中立な枠を台帳から直接構成する。

| 項目 | 規則 |
|---|---|
| 基準期間 | 2026年8月1日--8月31日（UTC）＝ block 25,656,292--25,878,704（222,412 ブロック） |
| 抽出 | 期間内から等間隔に 28 の窓、各 8 ブロック（計 224 ブロック、**0.10%**） |
| 稼働の指標 | 窓の中で当該アドレスが発火した記録の件数 |
| 下限 | 抽出内で **30 件未満**を除く |
| 判定する件数 | 記録の件数の**上位 200 件** |
| 判定の規則 | 第10.9節 表10.8 の順序。変更しない |

**当初案（300 窓 × 20 ブロック、2.8%）は成立しない。** 下見で密度を実測したところ、

```
1 ブロックあたり ログ 774 件、558 KB
20 ブロックの窓は「query returns too many logs: 20000」で拒まれる
```

であり、窓は最大 8 ブロックである。当初案は通信量 3.47 GB になる。
密度を 40 倍見誤っていた。

抽出率は 0.10% で足りる。96 ブロックの下見で既に

| 下限 | 該当アドレス |
|---|---|
| 1 件以上 | 3,982 |
| 3 件以上 | 1,730 |
| 10 件以上 | 596 |
| **30 件以上** | **222** |
| 100 件以上 | 72 |

であり、人手判定の上限 200 件に対して 30 件以上が 222 件ある。
抽出率を上げても判定できる件数は増えない。費用は **28 リクエスト、0.12 GB、5 分**。

**限界を明記する。** この枠は「事象を発火した識別子」であり、
イベントを出さない手続きは落ちる。分野には中立だが網羅ではない。

### 4.5 標本の判定（Φ3 と共通）

枠A と枠B の双方から抽出した手続きについて、一度の判定で両方の列を埋める。

| 列 | 埋めるもの | 予測 |
|---|---|---|
| `区分内` | 前提(2)を満たすか（保管型でないか） | 共通 |
| `族` `型` | 第10.9節の判定順序の結果 | Φ4 |
| `根拠` | どの判定で決まったか | Φ4 |
| `tau_符号` | $\pi$ が $\delta$ に先行（負）、同時（0）、後れる（正） | Φ3 |
| `tau_日数` | 後れる場合の平均的な遅れ | Φ3 |

### 4.6 枠B の取得結果

第4.4節の規則どおりに取得した（`data/scripts/phi/phi4_fetch_frameb.py`）。

| 量 | 値 |
|---|---|
| ブロック | 224（28 窓 × 8） |
| ログ | 170,545 件 |
| 相異なるアドレス | 6,633 |
| **下限 30 件以上** | **491** |
| 判定の対象（上位 200 件） | 200 |
| 所要 | 1.1 分 |

出力は `data/raw/phi/phi4_frameb.tsv`（`address` / `n_logs` / `n_topics0`）。

下見の見積もり（30 件以上が 222）を上回って 491 件あった。
下見は 96 ブロックだったのに対し本取得は 224 ブロックであり、
窓が増えれば同じアドレスが繰り返し現れて件数が伸びるためである。
**規則は変えない。** 上位 200 件が判定の対象である。

なお `n_topics0`（そのアドレスが発火した相異なるイベント種の数）も記録した。
判定のときに手続きの性格を推し量る材料になる。

### 4.7 判定順序の前に一段が要る

**表10.8 は $\Phi$ を所与としている。** 実際に台帳の識別子へ当てると、
そもそも $\Phi$ でないものが多数を占めた。履行 $\delta$ と決済 $\pi$ が
区別できないものは判定の対象にならない。

**判定0（本稿で追加）：これは $\Phi$ か。**
以下は $\Phi$ でないとして判定の外に置く。

| 除くもの | 理由 |
|---|---|
| トークンの台帳そのもの | 決済の媒体であって、$\delta$ と $\pi$ の対がない |
| なりすまし | 履行が存在しない |
| 対価を伴わない基盤 | $\pi$ がない（承認、保管、複数呼び出しの束ね、実行の入口） |

これは抽出規則の変更ではなく、定義4.1 の適用である。

### 4.8 結果：**予測Φ4 は文言としては外れた**

枠B の上位 200 件を第10.9節 表10.8 の順序で判定した
（`data/scripts/phi/phi4_classify.py` → `phi4_frameb_judged.tsv`）。

| 族 | 件数 | 主な中身 |
|---|---|---|
| **族6** 媒介 | **41** | DEX のルータとプール、集約器、橋渡し、Seaport、LI.FI、Relay、LayerZero |
| **族3** 資産の時間貸し | **13** | Morpho、Aave、Euler、Maker（Vat、PSM）、各種 Vault |
| **族5** 成果・状態連動 | 4 | Lido（stETH）、Rocket Pool（rETH）、Staked USDe、Numerai |
| **族2** 継続アクセス | 3 | VRF Coordinator、TWAP Oracle、UMA VotingV2 |
| **族7** 受益者と支払者の分離 | **1** | SingletonPaymasterV7 のみ |
| 族1、族4 | **0** | -- |
| $\Phi$ でない | 131 | 下記 |
| 判定不能 | 7 | 未検証で名前も無い（3.5%） |

判定できた 62 件のうち族7は 1 件（1.6%）である。族6 が 66%、族3 が 21% を占める。

**$\Phi$ でないとした 131 件（65.5%）の内訳。**

| 理由 | 件数 |
|---|---|
| ERC-20 の台帳そのもの | 71 |
| **決済媒体の名をまねたなりすまし** | **30** |
| 対価を伴わない基盤 | 23 |
| ERC-4337 の実行の入口 | 3 |
| ERC-721 の台帳 | 3 |
| 橋渡しの周辺部品 | 1 |

なりすまし 30 件は `ÚЅDТ` `ĖTḨ` `Ụ᠋5DT` `Tetheṛ USD` のように
Unicode の合成文字で USDT・USDC・ETH に似せたもので、
偽の `Transfer` を発火して財布の履歴に紛れ込ませる手口である。
**分野中立な枠を台帳から取ると、活動量の上位に詐取が大量に入る。**
これは枠A（DefiLlama）では起きない。枠の中立性と引き換えに入る雑音である。

**判定は当たっていない。** 予測Φ4 の文言は
「族7は 7-4 を除いてほぼ現れない」であった。
現れた 1 件は **7-2（クロスサブシディ）であり、7-4 は 0 件**である。
当たったのは「族7がほぼ現れない」という量的な部分だけで、
「残るのは 7-4 である」という部分は外れた。

**なぜ 7-4 が現れなかったか。** 7-4（寄付・支援）は $\kappa<0$ であり、
対価関係を明示しない。命題13.6 が言うのは受益者の\emph{同定}を要する $\pi$ が
書けないことであって、7-4 はその同定を要さないから実装できる、という論理であった。
実装できることと、稼働する $\Phi$ の上位に現れることは別である。
**「実装できる」から「現れる」を導いていたのが誤りである。**

**唯一の族7 を読んだ。**
`SingletonPaymasterV7`（`0x7777…834c`）はガス代の肩代わりであり、
利用者は無償、出資者が支払う 7-2 である。ソースを読むと
署名の検証が 11 箇所あり、**誰を対象にするかの判定は署名によって
オフチェーンに出している**（所有者による制御と名簿による制限は 0 箇所）。
命題13.6 が述べたとおり、識別の判断を台帳の外に出すことで成立している。

> **これを支持の証拠として扱わない。**
> 予測の文言は外れており、機構が命題と整合することは事後の説明である。
> 第1.3節が述べたとおり、記述の枠組みは観測されたどんな事実にも
> 事後の説明を与えられる。ここで踏みとどまるために予測を登録した。

**個別に読んで修正した判定。**

| 対象 | 当初 | 修正後 | 理由 |
|---|---|---|---|
| `EntryPoint` × 3 | 族7-2 | $\Phi$ でない | ERC-4337 の実行の入口。自らは $\delta$ も $\pi$ も持たない |
| `EthCreatorFeeHookV2` | 族7-4 | 族6-1 | 作成者への配分は手数料の分配であって受益者の分離ではない |
| `FeeForwarder` | 族7-4 | $\Phi$ でない | LI.FI の周辺部品。手数料の転送のみ |

### 4.9 進捗

- [x] 判定順序の確認
- [x] 枠A の取得と除外規則の適用
- [x] 母集団の偏りの同定
- [x] 下見による密度の実測と、抽出規則の確定（第4.4節）
- [x] 枠B の取得（491 件が下限を満たす）
- [x] 上位 200 件への注記の付与（Blockscout、193/200 で名前が取れた）
- [x] **判定と集計（外れ）**
- [ ] 枠A での判定（金融に偏る枠での対照）
- [ ] 本文への反映

## 5. 作業の記録

| 日付 | 内容 |
|---|---|
| 2026-09-04 | 本ファイルを起こす。データ源の到達性を確認。Φ1 の文献確認と密度測定、全期間走査を開始。Φ2 の一覧取得と TVL 系列の取得を開始。Φ4 の枠A を取得し偏りを同定 |
| 2026-09-04 | 節番号を `book.aux` で照合して訂正（判定順序は第13.5節ではなく第10.9節 表10.8、予測の登録は第13.6.3節、命題は12.2／13.2／13.6、系は13.12、注は13.15） |
| 2026-09-04 | Φ2 の手数料系列が完了（587/587）。Φ1 は 77% 通過、ログ 1,653,000 件。取り逃した区間を1件記録（第1.5節） |
| 2026-09-04 | Φ1 の全期間走査が完了（198 万件、138.7 分）。取り逃した8区間・2,000 ブロックを窓を詰めて全て補充。生存分析の入力を構成（改修 24,307 件／右打ち切り 190 万件） |
| 2026-09-04 | Φ2 の TVL 系列が完了（1,852/1,852）。暫定の閾値で途絶 78 件を抽出し、途絶前の傾きを集計（第2.5節）。**検定ではない**。閾値の確定と継続群との対照が要る |
| 2026-09-04 | 費用と無料枠を確認して記録（第0.6節）。認証情報を一切持たず、課金の経路が存在しないことを確かめた |
| 2026-09-04 | 下見の実測にもとづき、**取得の前に**三つの規則を確定。Φ1 の依存の数と統制と観測単位（第1.4節）、Φ2 の途絶の定義（第2.5節）、Φ4 の枠B の抽出（第4.4節）。いずれも結果を見ずに固定した |
| 2026-09-04 | **予測Φ2 を検定し、支持された**（第2.6節）。途絶前 30 日で途絶群の 75.0% が減少、継続群は 24.7%、$p=3.2\times10^{-9}$。継続群を置かなければ誤って支持と読む場面があった（180 日窓では分離しない） |
| 2026-09-04 | 枠B を取得（第4.6節）。224 ブロック、ログ 170,545 件、下限 30 件以上が 491 件 |
| 2026-09-04 | 実装バイトコード 76,609 件を取得（62.5 分）。依存の数を算出し、パネルの 100.0% に結合 |
| 2026-09-05 | **予測Φ3 は検証不能と判定**（第3.5節）。企業部門の $\CCC$ も 58 業種すべて非負であり、対抗仮説が識別されない。加えて区分「内」の $\tau$ は秒のオーダーで桁が五つ違う。個票（EDINET）を取る道は母集団が 3 桁狭くなるため採らない（第3.7節） |
| 2026-09-04 | **予測Φ4 を判定し、文言としては外れた**（第4.8節）。族7 は 1 件だが、それは 7-2 であって 7-4 は 0 件。判定順序の前に「これは $\Phi$ か」の一段が要ることも判った（200 件中 131 件が $\Phi$ でない。うち 30 件がなりすまし） |
| 2026-09-04 | **予測Φ1 を検定し、棄却された**（第1.8節）。全体では支持に見えるが、45,726 のクラスタから 1 つ（`AuthenticatedProxy`、曝露の 95%）を外すと消え、符号も逆転する。**クラスタ頑健標準誤差では点推定の偏りを防げなかった**（第1.4節の判断の誤り） |

---

## 6. 参照

- Qasse, I., Hamdaqa, M., Jónsson, B. Þ. (2024).
  [*Immutable in Principle, Upgradeable by Design: Exploratory Study of Smart Contract Upgradeability*](https://arxiv.org/abs/2407.01493). arXiv:2407.01493
- [ERC-1967: Proxy Storage Slots](https://eips.ethereum.org/EIPS/eip-1967)
  — 実装スロット `0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc`
- [blockchain-etl/ethereum-etl](https://github.com/blockchain-etl/ethereum-etl)
- [blockchain-etl/awesome-bigquery-views](https://github.com/blockchain-etl/awesome-bigquery-views)
- [paradigmxyz/paradigm-data-portal](https://github.com/paradigmxyz/paradigm-data-portal) — CC0、parquet
- [paradigmxyz/cryo](https://github.com/paradigmxyz/cryo)
- [Sourcify Docs](https://docs.sourcify.dev/docs/api/)
- [DefiLlama Docs](https://docs.llama.fi/)
- [Etherscan API v2 rate limits](https://docs.etherscan.io/etherscan-v2/rate-limits)
- Yokoo et al. (2004) false-name-proofness — 命題13.6（受益者）の対応（本文で引用済み）


---

## 7. 副次的に測れた量

検定の過程で、本稿が他の章で測定を断念していた量が測れた。
本文の第13.6.5節に反映した。

### 7.1 応答の率と $1/\lambda$

| 量 | 内容 | $\lambda$（毎年） | $1/\lambda$ |
|---|---|---|---|
| 改修の率 | 実装が差し替えられた率。193 万件、複製の集中を除く | 0.048 | **20.6 年** |
| 停止の率 | 稼働が絶えた率。1,718 件の全数 | 0.030 | **33.8 年** |

**測っているのは $\lambda_{\rm novel}$ ではない。** どちらも到来ではなく応答を数えている。
改修は運営者が応答した場合、停止は応答しなかったか、できなかった場合である。
応答を要さなかった到来はどちらにも現れないから、これらは下限であり、
**命題12.2 の $1/\lambda_{\rm novel}$ は 20 年より短い**という上限が得られる。

母集団が異なるため二つは加法的でない。

**分布は二峰である。** 改修された 20,732 件の中央値は 42 日、
改修されないまま観測を終えた 190 万件の中央値は 4.6 年である。
手を入れるものは直ちに入れ、入れないものは何年も入れない。

### 7.2 手続きの継続期間

第21章が「母集団フレームを欠く」として断念した量である。

```
母集団 1,718 件（Ethereum を含むプロトコルの全数）
  停止を観測 143 件（8.3%） / 継続 1,575 件（右打ち切り）
  停止したもの: 中央値 1.62 年、四分位 0.64--2.82、最大 6.0
  継続中     : 中央値 2.75 年、最大 8.9
```

停止したものを事後に集めたのではなく全数から数えているため、
第15.2節の生存条件づけは生じない。

種別ごとの停止率には順序がある（$1/\lambda$、年）。

```
役務 11.1 < 高倍率運用 11.5 < 発行支援 15.3 < 指数 18.1
     < 橋渡し 20.1 < 保険 22.3 < 貸付 22.6 < 利回り 22.7
```

**この順序を依存の数で説明しない。** 予測$\Phi$1 が棄却されている以上、
後付けの説明になる。

### 7.3 拘束された状態 $B$ と料率

表13.4 は $B$ について「対応する量を本稿は持たない」と記していた。
区分「内」では手続き上に置かれた資産として直接観測でき、1,718 件の日次系列が得られる。

$B$ が測れると料率（年間手数料 $\div$ $B$）が定義できる。
**ただし分母は族によって異なる。**
族3（資産の時間貸し）では $B$ が正しい分母だが、族6（媒介）では取引額が分母であり
$B$ は在庫にすぎない。混ぜると比が発散する（実測で最大 7,393,081\%）。

族3系に限り、$B$ が 100 万ドル以上のもの 144 件。

| 種別 | $n$ | 中央値 | 四分位 |
|---|---|---|---|
| 裁定 | 7 | 4.97\% | 2.00--9.31\% |
| 実物資産 | 20 | 3.85\% | 2.35--5.84\% |
| 担保付債務 | 15 | 3.53\% | 0.99--11.86\% |
| 流動化した預託 | 19 | 3.12\% | 2.58--4.21\% |
| 貸付 | 30 | 2.97\% | 1.77--4.68\% |
| 危険の管理 | 21 | 2.83\% | 2.02--4.47\% |
| 利回り | 15 | 2.72\% | 0.76--8.01\% |
| 利回りの集約 | 14 | 1.69\% | 0.90--4.19\% |

全体の中央値 3.07\%、四分位 1.77--5.84\%。

**第21章の料率の梯子とは比較できない。** あちらは取引額に対する率、
こちらは残高に対する率であり、次元が違う。

### 7.4 予測D と $\Phi$1 の重複

```
予測D  （第21.1節、第13段階）依存の少ない構成ほど λ_dep が小さく、無人運転可能期間が長い
予測Φ1（第13.6.3節、第16段階）呼び出す他の手続きの数が少ないほど、改修されるまでの期間が長い
```

**同一の関係を二度登録していた。** 登録の時点で気づいていなかった。

さらに第24段階まで、判定表は D を「部分的支持」としていたが、
本文にその判定を支える節がなく、第21.8節は「予測Dの検定に至っていない」と記していた。
**表と本文が矛盾していた。**

$\Phi$1 の検定が D の検定でもあるので、判定を**棄却**に改めた。
登録の一覧を章ごとに持ち、全体で突き合わせていなかったことによる運用上の失敗である。

### 7.5 依然として計算できないもの

新しいデータでも変わらなかったもの。

| 量 | 障害 |
|---|---|
| 主体単位の $\kappa_i$ | 集計の不能。**複製の集中という第二の形式が加わった** |
| $\phi_{\rm cog}$ | 個票と外生ショックに到達しない |
| $\phi_{\rm prod}$ と $\phi_{\rm barg}$ の分離 | $p^\ast$ が定義できない |
| 個人事業主の $\kappa$ | 集計の選択 |
| 役員報酬と労働時間 | 労働時間の統計が存在しない |
| 無人化率 | 測度が定義されない（不良設定） |
| $\lambda_{\rm novel}$ の四成分への分解 | 改修の内容を読む必要があり、検証済みに限られて標本が偏る |
